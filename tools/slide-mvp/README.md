# King Tamar Slide MVP

Pipeline:

```text
Slide JSON -> Hebrew QA -> editable PPTX renderer -> visual screenshot QA -> final asset
```

This folder contains the controlled production MVP for PRIME / King Tamar slides.

Key rule: image generation creates background and atmosphere; final Hebrew typography must be rendered in a deterministic layer.
